import 'package:flutter/material.dart';

class popular_items extends StatelessWidget {
  const popular_items({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
